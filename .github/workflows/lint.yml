name: Lint

on:
  - pull_request
  - push

jobs:
  lint:
    uses: houseabsolute/workflow-perl-dzil-ci/.github/workflows/lint.yml@v0
    with:
      ref: v0
      # File::LibMagic is a develop dep and needs the libmagic headers.
      apt-packages: libmagic-dev
